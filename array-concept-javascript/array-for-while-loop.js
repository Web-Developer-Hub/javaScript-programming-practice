// //looping an array use while loop....find out all  element
var languageName = ["JavaScript", "Python", "TypeScript", "C", "C++", "PHP", "Ruby", "CSharp", "Golang"];
i = 0;
while (i < languageName.length) {
    var result = languageName[i]
    console.log(result);
    // console.log(languageName[i]); // shorcut way
    i += 1;
}

console.log("program terminate");

//looping an array use while loop....find out even index value
var languageName = ["JavaScript", "Python", "TypeScript", "C", "C++", "PHP", "Ruby", "CSharp", "Golang"];
i = 1;
while (i < languageName.length) {
    var result = languageName[i]
    console.log(result);
    // console.log(languageName[i]); // shorcut way
    i += 2;
}

console.log("program terminate");

//looping an array use while loop....find out odd index value
var languageName = ["JavaScript", "Python", "TypeScript", "C", "C++", "PHP", "Ruby", "CSharp", "Golang"];
i = 0;
while (i < languageName.length) {
    var result = languageName[i]
    console.log(result);
    // console.log(languageName[i]); // shorcut way
    i += 2;
}